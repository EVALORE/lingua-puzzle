import { WordCardDirective } from './word-card.directive';

describe('WordCardDirective', () => {
  it('should create an instance', () => {
    const directive = new WordCardDirective();
    expect(directive).toBeTruthy();
  });
});
